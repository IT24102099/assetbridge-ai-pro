namespace AssetBridge.Domain.Enums;

// Represents the scheduling status of a service provider for a given time window.
public enum AvailabilityStatus
{
    Available = 1,
    Busy = 2,
    Unavailable = 3
}
