namespace AssetBridge.Domain.Enums;

// Priority level for long-term continuity tasks and scheduled rechecks.
public enum FollowUpPriority
{
    Low = 1,
    Medium = 2,
    High = 3,
    Critical = 4
}
